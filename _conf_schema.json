{
  "provider_id": {
    "description": "选择大模型提供商（留空=使用当前会话默认）",
    "type": "string",
    "_special": "select_provider",
    "default": "",
    "obvious_hint": true
  },
  "model": {
    "description": "可选：强制指定模型名（不填用提供商默认）",
    "type": "string",
    "default": ""
  },
  "option_count": {
    "description": "默认生成的选项个数（建议 3）",
    "type": "int",
    "default": 3
  },

  "render_image": {
    "description": "启用图片渲染（关闭则以纯文本输出）",
    "type": "bool",
    "default": true,
    "obvious_hint": true
  },
  "canvas_width": {
    "description": "画布宽度(px)",
    "type": "int",
    "default": 1280
  },
  "canvas_height": {
    "description": "画布高度(px)",
    "type": "int",
    "default": 720
  },
  "image_quality": {
    "description": "导出 JPEG 质量（10~100，越大越清晰体积越大）",
    "type": "int",
    "default": 85
  },
  "background_dir": {
    "description": "背景图目录（相对插件目录，支持 jpg/png/webp）",
    "type": "string",
    "default": "background"
  },
  "avatar_url_tmpl": {
    "description": "头像 URL 模板（用 {qq} 占位符替换为 QQ 号）",
    "type": "string",
    "default": "https://q1.qlogo.cn/g?b=qq&nk={qq}&s=640"
  },

  "prompt_template": {
    "description": "追加到系统提示后的模板（用于约束生成风格）",
    "type": "text",
    "editor_mode": true,
    "editor_theme": "vs-dark",
    "default": "这些选项必须代表主角的关键抉择，且在情感与立场上彼此强对比：例如 友好/敌对、热情/冷淡、理性/冲动、顺从/反抗、直球/逃避、温柔/傲娇。\n每个选项必须：\n1. 明确体现不同的性格取向与剧情走向（差异要大，语义不得接近）。\n2. 具有强烈的情感色彩，像主角的内心独白或决定，可适当添加（）表示动作。\n3. 每个选项在20字以内，情感动作倾向鲜明。\n4. 严格输出为多行纯文本，每行以 大写字母+点+空格 开头（如 A. ）。\n5. 不得输出任何命令或斜杠（/），不得包含系统说明。\n6. 符合QQ群聊说话风格"
  },

  "title": {
    "description": "（纯文本模式）消息标题",
    "type": "string",
    "default": "🎮 GalGame 选项"
  },
  "message_separator": {
    "description": "（纯文本模式）分隔线",
    "type": "string",
    "default": "-------------------------"
  },
  "show_quote": {
    "description": "（纯文本模式）是否显示引用原文",
    "type": "bool",
    "default": true
  }
}
